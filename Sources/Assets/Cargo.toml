[package]
name = "assets"
version = "0.1.0"
edition = "2024"

[dependencies]
rust-embed = "8.7.0"
swift-bridge = { version = "0.1.57", features = ["async"] }

[build-dependencies]
swift-bridge-build = { version = "0.1" }

[lib]
crate-type = ["staticlib", "rlib"]
