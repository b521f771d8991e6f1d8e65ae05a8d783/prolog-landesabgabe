[package]
name = "logic-kit"
version = "0.1.0"
edition = "2024"

[dependencies]
wasm-bindgen = { version = "0.2.100", optional = true }

[features]
default = []
wasm_interface = ["wasm-bindgen"]

[lib]
crate-type = ["cdylib", "staticlib", "rlib"]
